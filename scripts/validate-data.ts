/**
 * Validates the source corpus in /data/source against the Zod schema.
 * Reports duplicate IDs, orphaned categories, and missing Amharic translations.
 *
 * Phase 0 stub. Phase 1 will implement Zod schema + reporting.
 */
function main(): void {
  console.log('validate-data: not yet implemented (Phase 1).');
  process.exit(0);
}

main();
