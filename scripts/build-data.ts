/**
 * Transforms the validated source corpus in /data/source into the shipped
 * JSON files consumed by src/data/loader.ts:
 *   - prayers.en.json
 *   - prayers.am.json
 *   - categories.json
 *   - index.json
 *
 * Phase 0 stub. Phase 1 will implement the transform.
 */
function main(): void {
  console.log('build-data: not yet implemented (Phase 1).');
  process.exit(0);
}

main();
